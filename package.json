{
  "name": "thefehrs-learning-manager",
  "version": "5.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:docker": "node scripts/dev-docker.mjs",
    "dev:docker:fresh": "rm -rf foundry_data_dev && npm run dev:docker",
    "generate-types": "node scripts/generate-external-types.mjs",
    "build": "tsc && vite build",
    "typecheck": "tsc --noEmit",
    "fmt": "oxfmt --no-error-on-unmatched-pattern",
    "fmt:check": "oxfmt --check",
    "lint": "oxlint src tests",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e:docker": "bash -c 'set -o pipefail; npm run test:e2e:docker:verbose | grep -v -E \"Browser Log:|Browser Warning:\" | cat -s'",
    "test:e2e:docker:verbose": "npm run build && rsync -a --delete dist/ e2e/thefehrs-learning-manager/ && PLAYWRIGHT_CHROMIUM_EXECUTABLE_PATH=/snap/bin/chromium PLAYWRIGHT_HTML_REPORT=playwright-report-v14 PLAYWRIGHT_OUTPUT_DIR=test-results-v14 FOUNDRY_VERSION=14 foundry-playwright test --docker",
    "test:e2e:docker:v13": "bash -c 'set -o pipefail; npm run test:e2e:docker:v13:verbose | grep -v -E \"Browser Log:|Browser Warning:\" | cat -s'",
    "test:e2e:docker:v13:verbose": "npm run build && rsync -a --delete dist/ e2e/thefehrs-learning-manager/ && PLAYWRIGHT_CHROMIUM_EXECUTABLE_PATH=/snap/bin/chromium foundry-playwright test --docker",
    "test:e2e:verify": "bash scripts/e2e-verify.sh",
    "release": "release-it",
    "prepare": "husky"
  },
  "devDependencies": {
    "@league-of-foundry-developers/foundry-vtt-types": "^13.346.0-beta.20250812191140",
    "@playwright/test": "^1.59.1",
    "@release-it/bumper": "^7.0.5",
    "@sveltejs/vite-plugin-svelte": "^7.0.0",
    "@thefehr/foundry-playwright": "^0.6.0",
    "@vitest/coverage-v8": "4.1.3",
    "dotenv": "^17.4.2",
    "happy-dom": "^20.8.3",
    "husky": "^9.1.7",
    "lint-staged": "^16.3.2",
    "oxfmt": "^0.54.0",
    "oxlint": "^1.60.0",
    "release-it": "^19.2.4",
    "sass-embedded": "^1.97.3",
    "svelte": "^5.20.0",
    "typescript": "^5.0.0",
    "vite": "^8.0.1",
    "vitest": "4.1.3"
  },
  "lint-staged": {
    "**/*.{ts,json,js,scss,md,mjs,yml}": "npm run fmt",
    "{src,tests}/**/*.{ts,js}": "oxlint"
  },
  "engines": {
    "node": ">=20.19.0"
  }
}
